# web/dist is committed (see render.yaml) so Render's native Go build never
# needs Node. Rebuild with `npm --prefix ui run build` after UI changes.

# Local store snapshot.
/data/

node_modules/
.DS_Store


# Local test builds (Render builds its own).
/bin/
